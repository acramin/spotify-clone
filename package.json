{
  "name": "spotify-clone",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix back && npm install --prefix front && npm run build --prefix front",
    "start": "npm run start --prefix back && npm run dev --prefix front"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "spotify-clone": "file:"
  }
}
